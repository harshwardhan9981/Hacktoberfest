#include <stdio.h>
#include <stdlib.h>

int main()
{
    
    arr = (int*) malloc(num * sizeof(int));
    for(i = 0; i < num; i++) {
        scanf("%d"
              rray. */
    int temp=0;
    for(int i=0,j=num-1;i<=j;i++,j--){
        temp=arr[i];
        arr[i]=arr[j];
        arr[j]=temp;
    }

              
}

              
